os: Visual Studio 2015
configuration: Release

version: 0.0.0.{build}

assembly_info:
  patch: true
  file: AssemblyInfo.*
  assembly_version: "{version}"
  assembly_file_version: "{version}"
  assembly_informational_version: "{version}"

before_build:
  - nuget restore

build:
  verbosity: minimal
  cmd: msbuild Alegri.Data.EF6.sln /p:Configuration=Release /p:DeployOnBuild=true /p:Platform=AnyCPU

